<%
  var s = typeof status !== 'undefined' ? status : 'Unpaid';
  var cls = s === 'Paid' ? 'success' : (s === 'Overdue' ? 'danger' : 'warning text-dark');
%>
<span class="badge bg-<%= cls %>"><%= s %></span>
